# local: this browser only. supabase: deployed Edge Function + cloud Postgres.
VITE_ROOM_STORAGE=local
VITE_SUPABASE_URL=
# Public anon/publishable key only. Never put a service_role/secret key in VITE_*.
VITE_SUPABASE_ANON_KEY=
